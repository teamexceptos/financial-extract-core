[package]
name = "bill-extractor-core"
version = "0.1.0"
edition = "2021"

[lib]
name = "bill_extractor_core"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
